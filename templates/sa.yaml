apiVersion: v1
kind: ServiceAccount
metadata:
  name: unileaf-account
  namespace: neuro-san-{{ .Values.environment }}
  labels:
    azure.workload.identity/use: "true"
  annotations:
    azure.workload.identity/client-id: {{ .Values.azure.workloadIdentityClientId }}
