# Secrets are created directly as Kubernetes secrets (no Key Vault for hackathon).
# Run these commands once before helm install (replace <KEY_N> and <ENDPOINT_N> with real values):
#
# kubectl create secret generic openai-key-1 \
#   --from-literal=AZURE_OPENAI_API_KEY=<KEY_1> \
#   --from-literal=AZURE_OPENAI_ENDPOINT=<ENDPOINT_1> \
#   -n neuro-san-hackathon
#
# kubectl create secret generic openai-key-2 \
#   --from-literal=AZURE_OPENAI_API_KEY=<KEY_2> \
#   --from-literal=AZURE_OPENAI_ENDPOINT=<ENDPOINT_2> \
#   -n neuro-san-hackathon
#
# kubectl create secret generic openai-key-3 \
#   --from-literal=AZURE_OPENAI_API_KEY=<KEY_3> \
#   --from-literal=AZURE_OPENAI_ENDPOINT=<ENDPOINT_3> \
#   -n neuro-san-hackathon
#
# kubectl create secret generic openai-key-4 \
#   --from-literal=AZURE_OPENAI_API_KEY=<KEY_4> \
#   --from-literal=AZURE_OPENAI_ENDPOINT=<ENDPOINT_4> \
#   -n neuro-san-hackathon
#
# kubectl create secret generic openai-key-5 \
#   --from-literal=AZURE_OPENAI_API_KEY=<KEY_5> \
#   --from-literal=AZURE_OPENAI_ENDPOINT=<ENDPOINT_5> \
#   -n neuro-san-hackathon
#
# kubectl create secret generic openai-key-6 \
#   --from-literal=AZURE_OPENAI_API_KEY=<KEY_6> \
#   --from-literal=AZURE_OPENAI_ENDPOINT=<ENDPOINT_6> \
#   -n neuro-san-hackathon
#
# kubectl create secret generic azure-storage \
#   --from-literal=AZURE_STORAGE_CONNECTION_STRING="$(az storage account show-connection-string \
#     -g neuro-san-studio-marketplace-rg -n neurosanhackathonsa --query connectionString -o tsv)" \
#   -n neuro-san-hackathon
